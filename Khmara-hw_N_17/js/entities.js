function Product(id, name, price){
    this.id = id;
    this.name = name;
    this.price = price;
}

function Order(date, totSum, map){
    this.date = date;
    this.totSum = totSum;
    this.map = map;
}
